---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SITE, GITHUB } from '../consts';

const stack = [
  { group: '语言', items: ['C / C++', 'Java', 'JavaScript / TypeScript', 'Python'] },
  { group: '方向', items: ['数据结构与算法', '操作系统基础', '图形化编程', 'Web 前端'] },
  { group: '工具', items: ['Git / GitHub', 'VS Code', 'Astro', 'Node.js'] },
];
---

<BaseLayout title="关于" description="关于 hhkai：一个正在持续学习的开发者。">
  <header class="flex flex-col gap-6 sm:flex-row sm:items-center">
    <img
      src="/images/avatar.jpg"
      alt="hhkai 的头像"
      width="96"
      height="96"
      loading="lazy"
      class="h-24 w-24 shrink-0 rounded-full border border-line object-cover"
    />

    <div>
      <p class="font-mono text-xs uppercase tracking-[0.2em] text-accent">about</p>
      <h1 class="mt-2 text-3xl font-semibold">关于</h1>
      <p class="mt-2 text-muted">你好，我是 {SITE.author}。</p>
    </div>
  </header>

  <section class="article mt-10">
    <p>
      这里是我的个人网站，用来存放平时的学习笔记、一些练习项目，以及几个纯粹觉得好玩的实验。
      内容以编程基础为主 —— C++、数据结构、操作系统，也会有一些图形化和 Web 相关的记录。
    </p>
    <p>
      比起把笔记散落在各个 App 里，我更想把它们收拢到一个自己完全掌控的地方。
      所以这个站点用 Astro 搭起来，笔记就是本地仓库里的 Markdown 文件，写完
      <code>git push</code> 就自动发布。
    </p>
    <p class="text-muted">
      内容还在慢慢补。以前发过的旧文章也已经迁移过来了，有些还很粗糙，正在逐篇整理。
    </p>
  </section>

  <section class="mt-12">
    <h2 class="text-xl font-semibold">技术栈</h2>
    <dl class="mt-5 space-y-4">
      {
        stack.map((row) => (
          <div class="sm:flex sm:gap-6">
            <dt class="w-16 shrink-0 font-mono text-xs leading-7 text-muted">{row.group}</dt>
            <dd class="flex flex-wrap gap-1.5">
              {row.items.map((item) => (
                <span class="rounded-full border border-line px-2.5 py-0.5 font-mono text-xs text-muted">
                  {item}
                </span>
              ))}
            </dd>
          </div>
        ))
      }
    </dl>
  </section>

  <section class="mt-12">
    <h2 class="text-xl font-semibold">找到我</h2>
    <ul class="mt-5 space-y-2 text-sm">
      <li class="flex items-center gap-3">
        <span class="w-16 shrink-0 font-mono text-xs text-muted">GitHub</span>
        <a
          href={GITHUB.url}
          target="_blank"
          rel="noopener noreferrer"
          class="text-accent underline underline-offset-2"
        >
          @{GITHUB.username}
        </a>
      </li>
      <li class="flex items-center gap-3">
        <span class="w-16 shrink-0 font-mono text-xs text-muted">本站仓库</span>
        <a
          href={`${GITHUB.url}/${GITHUB.username}.github.io`}
          target="_blank"
          rel="noopener noreferrer"
          class="text-accent underline underline-offset-2"
        >
          {GITHUB.username}.github.io
        </a>
      </li>
      <li class="flex items-center gap-3">
        <span class="w-16 shrink-0 font-mono text-xs text-muted">订阅</span>
        <a href="/rss.xml" class="text-accent underline underline-offset-2">RSS</a>
      </li>
    </ul>
  </section>
</BaseLayout>
